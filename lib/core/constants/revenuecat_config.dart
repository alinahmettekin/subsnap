/// RevenueCat configuration.
/// Get your API keys from: https://app.revenuecat.com → Project → API Keys
class RevenueCatConfig {
  /// Public API key (test key for development).
  /// Replace with production keys per platform when ready.
  static const String apiKey = 'test_MgMoqmGrqkkpybtjHFpAyJquaGZ';

  /// Entitlement identifier for SubSnap Pro.
  /// Must match the entitlement ID in RevenueCat Dashboard.
  static const String proEntitlementId = 'pro';
}
